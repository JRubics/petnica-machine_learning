# cos = np.cos(4)
		# sin = np.sin(4)
		# aa -= center[0]
		# bb -= center[1]
		# xa -= center[0]
		# xb -= center[1]
		# x = aa * cos - bb * sin
		# y = aa * sin + bb * cos
		# pa = xa * cos - xb * sin
		# pb = xa * sin + xb * cos
		# aa = x + center[0]
		# bb = y + center[1]
		# xa = pa + center[0]
		# xb = pb + center[1]
		# #a = xa,aa 		b = xb,bb
		# g = (c[0]-xa)*(c[0]-xa) + (c[1]-xb)*(c[1]-xb)
		# s = math.sqrt(g)
		# cv.circle(img,(c[0],c[1]),int(s),(0,255,0), 2)